import { Component } from '@angular/core';

@Component({
    templateUrl: './picker.component.html',
})
export class PickerComponent {
    timeValue?: string;
    dateValue?: string;
    yearValue?: string;
}
